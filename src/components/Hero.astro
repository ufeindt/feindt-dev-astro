<section id="hero">
  <div>
    <h1>Hi, I'm Ulrich Feindt</h1>
    <p>Full Stack Developer & Problem Solver</p>
    <p>
      I build modern web applications and bring creative ideas to life through
      code.
    </p>
    <div>
      <a href="#contact"> Get in Touch </a>
      <a href="#projects"> View Projects </a>
    </div>
  </div>
</section>

<style>
  section {
    align-items: center;
    display: flex;
    min-height: 100vh;
    text-align: center;
  }

  section > div > h1 {
    color: var(--color-text-primary);
    font-family: var(--font-display);
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 2rem;
  }

  section > div > p:first-of-type {
    color: var(--color-text-secondary);
    font-family: var(--font-display);
    font-size: 1.25rem;
    margin-bottom: 2rem;
  }

  section > div > p:last-of-type {
    color: var(--color-text-tertiary);
    font-family: var(--font-sans);
    font-size: 1.125rem;
    margin-bottom: 3rem;
    margin-inline: auto;
  }

  section > div > div {
    display: flex;
    justify-content: center;
    gap: 1rem;
  }

  section > div > div > a {
    border-radius: 0.5rem;
    text-decoration: none;
    font-family: var(--font-display);
    font-weight: 600;
    padding-block: 0.75rem;
    padding-inline: 2rem;
    transition: 0.3s;
  }

  section > div > div > a:first-of-type {
    background-color: var(--color-accent);
    color: var(--color-text-primary);
  }

  section > div > div > a:hover:first-of-type {
    background-color: var(--color-accent-hover);
  }

  section > div > div > a:last-of-type {
    border: 1px solid var(--color-accent);
    color: var(--color-accent-hover);
  }

  section > div > div > a:hover:last-of-type {
    background-color: var(--color-accent);
    color: var(--color-text-primary);
  }

  @media (width >= 48rem) {
    section > div > p:first-of-type {
      font-size: 1.5rem;
    }

    section > div > p:last-of-type {
      font-size: 1.25rem;
    }

    section > div > h1 {
      font-size: 3.75rem;
    }
  }
</style>
